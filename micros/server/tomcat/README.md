## Getting Started
> Build locally with JDK8 and Maven installed.

* git clone xxx

* cd xxx/demo

* mvn clean install

* cd target

# 命令启动应用程序
* java -jar target/micros_spring-0.0.1-SNAPSHOT.jar
* java -jar -Dserver.port=8090 target/micros_spring-0.0.1-SNAPSHOT.jar
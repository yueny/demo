package com.yueny.demo.dynamic.scheduler;

/**
 * 应用实例的可用状态
 *
 * @author yueny09 <deep_blue_yang@163.com>
 *
 * @DATE 2016年1月29日 上午12:08:33
 *
 */
// @Slf4j
@Deprecated
public class ApplicationEnabler {
	// public boolean enable() {
	// final ApplicationInstance instance =
	// instanceRepository.findByGuid(guid, ApplicationInstance.class);
	// if (instance.enabled()) {
	// log.debug("<{}> Instance[guid={}] already enabled, ignore it",
	// username(), instance.guid());
	// return false;
	// }
	//
	// final boolean addSuccessful = startupMonitoringJob(instance);
	// if (!addSuccessful) {
	// log.debug("<{}> NOTE: Add MonitoringJob[jobName={}] failed",
	// username(), instance.jobName());
	// return false;
	// }

	// // update
	// instance.enabled(true);
	// log.debug(
	// "<{}> Update ApplicationInstance[guid={}] enabled=true,jobName={}",
	// username(), instance.guid(), instance.jobName());
	//
	// return true;
	// }

}
